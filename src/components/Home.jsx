import React, { useState } from 'react'


const Home = (props) => {
    return (
        <div>
            <button>添加用户</button>
            <button>重置列表</button>
        </div>
    )
}

export default Home;